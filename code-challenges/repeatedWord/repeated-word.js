'use strict';

// Step 1: Split string on space and/or punctuation
// Step 2: Check if value is in hashtable, if it is then return value else add it to hashtable

let str = 'Once upon a time, there was a brave princess who.';

function repeatedWord(str) {
  str.split('');
}

let arr = str.split(' ');
console.log(arr);